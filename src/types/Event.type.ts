export default interface Event {
  id: number,
  name: string,
  location: string,
  start: string,
  end: string,
  description: string,
  guestsNumber: string,
  created: string,
  state: boolean,
}
